@page
@model Week3_BankAccount.Pages.Accounts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BankAccount[0].CurrentBalance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BankAccount[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BankAccount[0].IRSReportingNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BankAccount[0].AccountholderID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BankAccount) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentBalance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IRSReportingNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AccountholderID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Number">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Number">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Number">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
